---
- name: sftp-playbook | Add ldap User and group role
  hosts: artemis
#  become: yes
#  become_user: thulana
#  become_method: sudo
  vars_files:
      - vars/ftp_automation_vars.yml
  roles:
    - deploy

